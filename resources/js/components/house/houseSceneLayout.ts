/** Shared clip paths aligned to the house life-scene PNG layout. */
export const HOUSE_SCENE_BUILD_ZONE_CLIP = 'inset(0 0 7% 0)';
export const HOUSE_SCENE_PLATFORM_CLIP = 'inset(58% 0 0 0)';
/** Region above the stone platform band — used for build effects. */
export const HOUSE_SCENE_EFFECTS_BUILD_CLIP = 'inset(0 0 42% 0)';

/** Worker row on the front patio edge, backs toward the viewer, working upward toward the house. */
export const HOUSE_SCENE_WORKER_ROW_BOTTOM = '7%';
/** Platform band plus headroom above for raised tools. */
export const HOUSE_SCENE_WORKERS_CLIP = 'inset(48% 0 0 0)';
